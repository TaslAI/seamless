// This function waits for the DOM to load before executing
document.addEventListener('DOMContentLoaded', function()
{
  // Target the element by ID and change its text content
  var labelElement = document.getElementById('112036845');
  if (labelElement)
  {
    labelElement.textContent = 'Your New Text Here'; // Replace with the text you want
  }
});
